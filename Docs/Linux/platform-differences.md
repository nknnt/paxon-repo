## Platform Differences

The Windows build and Linux build use different packages and runtime implementations.

As a result, the runtime behavior and development environment may vary between platforms.